---
apiVersion: v1
kind: Service
metadata:
  name: kimi-k2-llamacpp
  namespace: llm-models-llamacpp
  labels:
    app: kimi-k2-llamacpp
    component: inference-server
  annotations:
    description: "OpenAI-compatible API service for Kimi K2 via llama.cpp"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: kimi-k2-llamacpp
    component: inference-server
  sessionAffinity: None
